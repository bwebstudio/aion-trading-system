"""
aion.analytics — Replay analytics, metrics, and parameter sweeps.

Public API:
  compute_metrics()       — aggregate statistics from a replay run
  build_report()          — structured report with per-session/regime/direction breakdowns
  run_parameter_sweep()   — grid search over engine parameters
"""
